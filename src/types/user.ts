export interface UserData {
  userId: string
  login: string
  accessToken: string
  refreshToken: string
}

export interface SetUserPayload {
  userId: string
  login: string
  accessToken: string
  refreshToken: string
}
